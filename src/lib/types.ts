/**
 * Domain DTO types (panel / free-API shapes).
 * Split under ./types/* — re-export for stable `@/lib/types` imports.
 * Use explicit `/index` so resolution does not prefer this file over the folder.
 */
export * from './types/index'
